---
status: redirect
---
# ZGW API Mapping

## Purpose
This spec is a redirect stub. The canonical specification for ZGW API mapping is owned by the Procest app at `procest/openspec/specs/zgw-api-mapping/spec.md`. This stub exists to preserve the spec slug locally and MUST NOT be treated as authoritative.

## Requirements

### Requirement: Consult the canonical zgw-api-mapping spec
Implementers MUST consult the canonical specification owned by Procest instead of treating this stub as authoritative.

#### Scenario: Locating the canonical spec
- **WHEN** a developer needs the requirements for ZGW API mapping
- **THEN** they MUST refer to `procest/openspec/specs/zgw-api-mapping/spec.md`
- **AND** they MUST NOT derive normative behavior from this stub
