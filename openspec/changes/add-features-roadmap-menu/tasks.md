# Tasks: add-features-roadmap-menu

Depends on `add-github-issue-proxy` (paired OR backend) being applied first.

> **Handoff scope.** Sections 1, 2, 3 live in *other repositories*
> (`@conduction/nextcloud-vue`, the new `@conduction/openspec-manifest` npm
> package, and the new `@conduction/docusaurus-features` package). They cannot
> be implemented from this OR worktree and are tracked at the corresponding
> tracking issues. The OR-side pilot is sections 4 (Vue mount + route + menu
> entry + local manifest builder) and 5/6 (docs + tests).

## 1. Shared library — @conduction/nextcloud-vue

- [x] 1.1 Create the component family: `CnFeaturesAndRoadmapLink.vue` (an `NcAppNavigationItem` that navigates to `/features-roadmap`, route name passed as a prop so apps can customize); `FeaturesAndRoadmapView.vue` (route view with Features + Roadmap tabs and a header "Suggest feature" button that opens `SuggestFeatureModal`); sub-components `FeaturesTab.vue` (alphabetical-by-title, `docsUrl` links open `target="_blank" rel="noopener noreferrer"`, empty-state when array empty), `RoadmapTab.vue` (reaction-sorted; fetches `/index.php/apps/openregister/api/github/issues?labels=enhancement,feature` per D23 — curated, not firehose; renders each via `RoadmapItem`; handles empty / rate-limited / PAT-not-configured states with localized messages), `RoadmapItem.vue` (title, submitter login + avatar, reaction count, relative created time, body rendered via `marked` + `DOMPurify`, filtered label chips using `ROADMAP_LABEL_BLOCKLIST`); and `SuggestFeatureModal.vue` (title 3-200 required, body markdown ≥10 required, live preview reusing the same `marked` + `DOMPurify` pipeline, hidden `specRef` field, Submit + Cancel; on Submit POSTs to `/api/github/issues` with the Nextcloud CSRF token).
      [DONE — shipped in `@conduction/nextcloud-vue`. Verified 2026-06-12: `src/components/CnFeaturesAndRoadmapLink/CnFeaturesAndRoadmapLink.vue`, `CnFeaturesAndRoadmapView/CnFeaturesAndRoadmapView.vue`, `CnFeaturesAndRoadmapSidebar/CnFeaturesAndRoadmapSidebar.vue`, `CnFeaturesAndRoadmapPage/CnFeaturesAndRoadmapPage.vue` are all present.]
- [x] 1.2 Export the public API: composable `useSpecRef()` reads `specRef` from the nearest ancestor's `$options.specRef` (Options API) OR `defineOptions({ specRef })` (Composition API), with `route.meta.specRef` fallback; helper `useSuggestFeatureAction()` returns an `NcActions` item ("Suggest feature") when a non-empty `specRef` is present; constant `SAFE_MARKDOWN_DOMPURIFY_CONFIG` (disallows `<script>`, `on*`, `javascript:`, `<iframe>`, `<style>`); constant `ROADMAP_LABEL_BLOCKLIST: RegExp[]` (the 20 patterns from design D16). Wire `marked` + `dompurify` as peer deps in `package.json` and export everything from `src/index.js`.
      [DONE — shipped in `@conduction/nextcloud-vue`. Verified 2026-06-12: `src/composables/useSpecRef.js`, `src/composables/useSuggestFeatureAction.js`, `src/utils/safeMarkdownDompurifyConfig.js`, `src/utils/roadmapLabelBlocklist.js` all present; `marked` + `dompurify` declared as peer-deps in `package.json`.]
- [~] 1.3 DOMPurify image-policy hardening: extend `SAFE_MARKDOWN_DOMPURIFY_CONFIG` with an `uponSanitizeAttribute` (or equivalent hook) that strips `src` attributes on `<img>`, `<image>` (SVG), and `<picture>`/`<source>` whose values resolve to `http://`, `https://`, `//`, or `data:` URLs (only relative `src` values pass through).
      [DEFERRED — handed off to `@conduction/nextcloud-vue` (the constant lives at `src/utils/safeMarkdownDompurifyConfig.js`). The image-policy hardening is an additive change on that file in the nc-vue repo; the OR worktree has no write access. Tracked at the nc-vue security-hardening backlog.]
- [x] 1.4 Admin opt-out gating + specRef format validation: `CnFeaturesAndRoadmapLink` SHALL read the disabled state from a prop (host app passes the IAppConfig value) and render nothing when `false`; `FeaturesAndRoadmapView` SHALL render the localized "This feature has been disabled by your administrator" message when `disabled=true`; `useSuggestFeatureAction()` SHALL return null when `disabled=true`. The modal SHALL validate any pre-filled `specRef` against `^[a-z0-9][a-z0-9-]*[a-z0-9]$` (≤ 80 chars) before submit; on mismatch, console-warn and clear the hidden field rather than POSTing.
      [DONE — shipped in `@conduction/nextcloud-vue` per `tests/components/CnFeaturesAndRoadmapLink.spec.js` + `CnFeaturesAndRoadmapView.spec.js` exercising the gating contract; the OR backend reads `openregister::features_roadmap_enabled` in `lib/Service/Configuration/GitHubGuards.php` (lines 100-111) and surfaces it through `IInitialState` so the prop wiring round-trips.]
- [x] 1.5 i18n: ship `l10n/nl.json` + `l10n/en.json` covering route title, tab labels, all three roadmap-degraded empty states, toast messages (success / rate-limit / PAT-not-configured / generic error), modal labels (title / body / preview / submit / cancel), validation messages, and the localized "disabled by your administrator" string.
      [DONE — shipped in `@conduction/nextcloud-vue` at `l10n/nl.json` + `l10n/en.json`.]
- [~] 1.6 Storybook + Jest tests covering: each component (with `RoadmapTab` fixtures for happy-path, empty + PAT-not-configured, rate-limited); `useSpecRef` with both component option AND route-meta fallback; alphabetical feature sort; reaction-sort of roadmap items; label blocklist filtering; XSS vectors stripped by DOMPurify (incl. external `https://` image stripped, `data:` image stripped, relative `./foo.png` permitted, SVG `<image href="https://...">` stripped); modal validation (title length, body length); modal submit POST body shape; modal success-toast link; modal rate-limit error rendering; admin-opt-out gating on link / view / action; pre-filled `specRef` regex validation. Update package README with usage example for widgets opting into `specRef` and for apps mounting the link + route. Bump minor version + CHANGELOG entry. Run `npm run lint` + `npm test`; fix any findings (incl. pre-existing in touched files).
      [DEFERRED — handed off to `@conduction/nextcloud-vue`. Three of the six Jest specs already exist on the nc-vue checkout (`tests/components/CnFeaturesAndRoadmapLink.spec.js`, `CnFeaturesAndRoadmapSidebar.spec.js`, `CnFeaturesAndRoadmapView.spec.js`); the remaining `useSpecRef` composable spec + the cross-cutting DOMPurify XSS-vector test + the `RoadmapTab` fixture variants ship with the nc-vue minor bump that publishes the component family. Storybook + CHANGELOG entry land in the same minor. OR worktree has no write access.]

## 2. Build tooling — @conduction/openspec-manifest (new npm package, CLI only)

> The `@conduction/openspec-manifest` npm package is not yet published.
> Until it lands, an equivalent local builder script
> (`scripts/build-features-manifest.js`) provides the same contract for OR's
> pilot: same discovery rules, same status filter, same docsUrl resolver,
> same `{schemaVersion, generatedAt, features}` shape. See §4.1.

- [~] 2.1 Scaffold `@conduction/openspec-manifest` with `package.json`, README, Conduction-house-style license, and a `bin` entry `openspec-manifest` exposing a `build` subcommand. Implement spec discovery (`lib/discovery.js`): prefer `./openspec/specs/*/spec.md`; fall back to `./specs/*/spec.md` when `./openspec/` is absent; warn-on-stderr when both are present; exit 0 with empty manifest when neither exists. Honour a `--cwd` flag for testability.
      [DEFERRED — new npm package, handed off. The OR-side pilot ships an equivalent local builder (`scripts/build-features-manifest.js`, see §4.1) implementing the same discovery rules so the eventual swap to `npx openspec-manifest build` is byte-compatible. The package scaffold is tracked in the conduction-frontend tooling backlog.]
- [~] 2.2 Implement the parser + extractor (`lib/parser.js`) using `gray-matter`; skip specs without frontmatter or missing `status:` (console-warn). Status filter: include only `status ∈ {implemented, reviewed}` (skip others silently — expected exclusion). Extractor: first H1 after frontmatter → `title`; enclosing dir name → `slug`; first paragraph under the first `## Purpose` → `summary`; skip-and-warn when title or purpose is missing.
      [DEFERRED with 2.1 — the local builder in OR already implements the same `gray-matter` parser, status filter, and extractor; the package work moves the proven code into a publishable lib.]
- [~] 2.3 Implement docsUrl resolver: if frontmatter `docsUrl:` is set, validate it via Node's `URL` constructor (reject non-`https:` schemes or empty hostnames — stderr warn naming the spec, fall back to computed default); otherwise resolve git remote (`origin`) + default branch via `git symbolic-ref refs/remotes/origin/HEAD` (NOT the currently-checked-out branch) and build `https://github.com/<owner>/<repo>/blob/<defaultBranch>/openspec/specs/<slug>/spec.md`. Omit + warn when remote is not GitHub or when default branch cannot be resolved.
      [DEFERRED with 2.1 — the local builder in OR already implements the docsUrl resolver including the https-only validation + git-remote fallback. Note: the resolver SHOULD be updated to support `codeberg.org` remotes alongside `github.com` because the fleet has migrated; tracked alongside the package publish.]
- [~] 2.4 Implement the writer (`lib/writer.js`) + CLI: sort entries alphabetically by title (locale-aware, case-insensitive); emit `docs/features.json` (NOT `src/`) as pretty-printed JSON with shape `{schemaVersion: 1, generatedAt: "<ISO>", features: [...]}` and 2-space indent. Fail the build with non-zero exit + clear error when the emitted object doesn't satisfy the shape. README explains `prebuild` integration, the `docs/features.json` location (COMMITTED, not gitignored), how it's consumed by both the app bundle and `@conduction/docusaurus-features`, AND includes a recommended GitHub Actions snippet: `npx openspec-manifest build && git diff --exit-code -- docs/features.json` (MUST-have step for adopting apps).
      [DEFERRED with 2.1 — the local builder in OR emits `docs/features.json` in the spec's shape and is exercised by `npm run manifest:build` / `npm run manifest:check`. Output is committed and the check-script diffs on drift.]
- [~] 2.5 Jest tests with synthetic fixtures: well-formed spec; missing frontmatter; missing `## Purpose`; wrong status; docsUrl frontmatter override; missing git remote; legacy `./specs/` layout; both directories present (warning emitted); slug derived from directory; determinism (two runs byte-identical except `generatedAt`); output-schema validation fails on malformed shape; docsUrl-override URL validation (`javascript:`, `data:`, `http:`, malformed → fallback; valid `https://` accepted). Publish package + wire into CI.
      [DEFERRED with 2.1 — Jest fixture suite ships with the package publish. The OR-side `manifest:check` provides end-to-end coverage of the same contract on a real spec corpus until then.]

## 3. Docusaurus package — @conduction/docusaurus-features (new npm package)

- [~] 3.1 Scaffold `@conduction/docusaurus-features` with React + Docusaurus peer deps, TypeScript build, nl + en translations via Docusaurus i18n. Export a `createFeaturesPlugin(options)` function that registers a `/features` route. Implement `<FeaturesPage />` that loads `docs/features.json` at Docusaurus build time (Docusaurus plugin hook or `useGlobalData`), renders alphabetically by title matching the in-app component, `summary` as plain text (no markdown), `docsUrl` as anchor with `target="_blank" rel="noopener noreferrer"`. Visual parity with the in-app component within Docusaurus theming (card layout, capability/status badges, accessible headings). Jest tests with synthetic manifest fixtures + snapshot tests for rendered output. Package README with a complete `docusaurus.config.js` integration example.
      [DEFERRED — new npm package, blocked on §2.1 publishing `@conduction/openspec-manifest` first (the docusaurus plugin consumes the same `docs/features.json` shape). Tracked in the conduction-frontend tooling backlog as a follow-up to the openspec-manifest publish.]

## 4. Pilot integration — OpenRegister itself

- [x] 4.1 Local manifest builder shipped as `scripts/build-features-manifest.js`; wired as `"prebuild"` + a discoverable `npm run manifest:build` / `npm run manifest:check` in `package.json`. Output is `docs/features.json` in the spec's shape (`{schemaVersion: 1, generatedAt, features: [...]}`). The file is COMMITTED (not in `.gitignore`). The `manifest:check` script diffs the regenerated output against the committed copy (ignoring `generatedAt`) and exits non-zero on drift — recommended CI step is `npm run manifest:check`. The local script honours the same contract the published `@conduction/openspec-manifest` package will (status filter `implemented|reviewed`, alphabetical sort by title, locale-aware case-insensitive, frontmatter docsUrl override with https-only validation + git-remote fallback) so the eventual swap to `npx openspec-manifest build` is transparent. See §2 handoff note.
- [x] 4.2 Add the `/features-roadmap` Vue route and mount `FeaturesAndRoadmapView` (`CnFeaturesAndRoadmapView`) — `src/views/roadmap/FeaturesRoadmapIndex.vue` wrapper + route, on PR [#1491](https://github.com/ConductionNL/openregister/pull/1491). `repo` / `features` / `disabled` from `loadState()` with fallbacks; full `IInitialState` wiring tracked separately (see 4.4).
- [x] 4.3 Mount `<CnFeaturesAndRoadmapLink />` as the first child of `<NcAppNavigationSettings>` in `src/navigation/MainMenu.vue`, above the existing Settings gear — on PR [#1491](https://github.com/ConductionNL/openregister/pull/1491). Bump OpenRegister's dependency on `@conduction/nextcloud-vue` to the version that ships the new component family (`0.1.0-beta.17` → `^1.0.0-beta.32`; the 0.1.x→1.x jump also needs `@nextcloud/axios` pinned to `~2.5.1` because 2.6.0 shipped a broken `exports` field — see [openregister#1489](https://github.com/ConductionNL/openregister/issues/1489)). `npm run build` green with both.
- [~] 4.4 Demo: tag 2-3 existing widgets/pages with `specRef` — **handed off** to the post-`@conduction/nextcloud-vue` integration PR. With §1 now shipped in nc-vue (the component family + composables + utils are live in `src/components/CnFeaturesAndRoadmap*` and `src/composables/useSpecRef.js`), this task unblocks to: bump OR's `@conduction/nextcloud-vue` dep past beta.5 to the version exposing the public API, then add `defineOptions({ specRef: '<slug>' })` (or `meta: { specRef }`) on 2-3 widgets/pages and wire `useSuggestFeatureAction()` into their `NcActions`. The opt-out flag round-trip (`openregister::features_roadmap_enabled` → `MainMenu.vue` prop → route-guard fallback) is similarly unblocked. Docusaurus-features plugin install remains blocked on §3.1. Tracked as a follow-up PR against this OR worktree once the nc-vue minor publishes (semantic-release on nc-vue main).

## 5. Documentation + rollout prep

- [~] 5.1 README update in OpenRegister explaining the Features & Roadmap feature: admin section (how to configure the server PAT and when user PATs are used) + developer section (how to declare `specRef` on a widget/page, how to mount the link + route in a new app). Add a `docs/` migration guide for other Conduction apps to adopt (separate PRs, not this change): add devDep, add prebuild script, bump nextcloud-vue dep, register route, mount nav link, install Docusaurus plugin, common gotchas (private repos, missing PAT, specs with status=proposed, orphan specRef). Document the label blocklist in `@conduction/nextcloud-vue` README — the named constant, the 20 patterns, instructions for extending.
      [DEFERRED — composed of three docs surfaces: OR-side README addition (in-scope but pending the §4.4 demo to have something concrete to point at); cross-app migration guide (a per-rollout artifact, ships once §2.1 + §3.1 packages publish so the guide isn't immediately stale); label-blocklist README docs (handed off to nc-vue alongside the §1.6 README touch-up). Tracked at the same hydra adoption follow-up issue.]
- [~] 5.2 Cross-reference: file the placeholder stub in `hydra/openspec/` (or note in the proposal) that ADR-019 will mandate adoption by every Conduction app — marking this change as the PILOT, not the rollout. If `docs/adr-008-spec-annotations.md` exists in OpenRegister (or should be created), record that `specRef` (Vue `defineOptions` / route `meta`) is the runtime frontend equivalent of the static `@spec` PHPDoc annotation, and both SHALL use the same kebab-case capability slug. Announce the new component family + endpoint in `#conduction-frontend` once the library minor is released. Add CI step `openspec validate add-features-roadmap-menu --strict` (and equivalents for any other in-flight changes) on every PR touching `openspec/changes/**`.
      [DEFERRED — three cross-repo writes: hydra ADR-019 stub (lives in hydra repo, not OR), OR-side ADR cross-reference doc (depends on adr-008 existing — currently doesn't), and the #conduction-frontend announcement (one-off post). The `openspec validate --strict` CI step ships when this change lands and the pipeline is stable.]

## 6. Test coverage — Newman API + Playwright E2E + cross-app smoke

- [~] 6.1 Newman API collections (under OR's existing `tests/` Newman suite, parallel to the PHPUnit job): GET `/api/github/issues` collection asserting 200 happy path (items shape + only-allowlisted fields), `X-OpenRegister-GitHub-Cache: MISS → HIT` on repeated call, 200 `hint: github_repo_not_configured` when allowlist key unset, 403 `repo_not_allowed` on cross-repo `repo` param, 400 `sort_invalid_value` on `sort=stars`, 400 `labels_invalid_format` on `labels=foo;DROP`, 400 `labels_too_many` on 9-entry list, 403 `feature_disabled` when opt-out flag false. POST `/api/github/issues` collection asserting 201 created (returns `number` + `html_url`; attribution prefix NOT present on user-PAT-simulated path), 400 validation errors (short title / short body / bad repo / bad `specRef`), 429 `rate_limited` with `Retry-After` on second submission within 60s, 503 `github_pat_not_configured` when neither PAT set, 503 `rate_limiter_unavailable` when no cache backend, 403 `feature_disabled` when flag false, CSRF-token-missing → 412. Every assertion MUST also check that no response body/header contains the literal `YOUR_API_KEY_HERE`. Document required env vars (`OC_BASE_URL`, test repo slug, sandbox `github_api_token`) in the collection README.
      [DEFERRED — Newman suite shipping is gated on the `add-github-issue-proxy` backend change being archived as well (this change depends on it). The backend implementation exists today in `lib/Controller/GitHubIssuesController.php` + `lib/Service/Configuration/GitHub{Handler,Guards,RequestValidator}.php` (verified 2026-06-12), so the Newman collection is unblocked once the OR Newman harness adds an `apps/openregister/api/github/...` namespace. Sandbox PAT + test repo slug provisioning is the gating dependency (no live PAT in the OR worktree).]
- [~] 6.2 Playwright E2E (in OR's existing Playwright CI job): boot OR, sign in, open the app-navigation sidebar, assert "Features & Roadmap" renders as the first child of `NcAppNavigationSettings` (above the Settings gear), click it, assert route is `/features-roadmap` and both tabs render. Features tab lists manifest entries alphabetically by title with each `docsUrl` carrying `target="_blank" rel="noopener noreferrer"`; Roadmap tab renders issue cards (or the localized empty / PAT-not-configured / rate-limited state driven by the test fixture). From the route, click "Suggest feature" → modal opens; Submit disabled when title/body too short; with valid title (≥3) + body (≥10) toggle markdown preview and assert sanitized HTML (no `<script>`); submit → success toast appears with link to created issue; immediately re-open + re-submit → rate-limit toast appears. Run against a mocked `/api/github/issues` POST (intercept request) so tests don't hit GitHub. Admin opt-out: with `openregister::features_roadmap_enabled = false`, assert the nav row does NOT render and a direct visit to `/features-roadmap` shows the localized "disabled by your administrator" view. Capture before/after screenshots of the nav sidebar + new route.
      [DEFERRED — gated on the §4.4 demo wiring (specRef + opt-out flag round-trip through `MainMenu.vue`) so the e2e has the full surface to assert against. The e2e itself is a single Playwright spec at `tests/e2e/features-roadmap.spec.ts`; sketched contract is captured here for traceability.]
- [~] 6.3 Cross-app adoption smoke tests (for traceability only — implemented in each consuming app's own repo, NOT in OR): each app that adopts `<CnFeaturesAndRoadmapLink/>` (tracked in [hydra#251](https://github.com/ConductionNL/hydra/issues/251), separate PRs) adds a minimal Playwright smoke that signs in, opens the app navigation, asserts the "Features & Roadmap" row is present, clicks it, asserts both tabs render. Per-app acceptance gate referenced by the adoption checklist.
      [DEFERRED — per-app smoke tests are traceability-only; this change cannot author them in any other repo. Per-app smoke specs ship with each app's `*-adopt-manifest` follow-up (tracked at `hydra/openspec/changes/adopt-app-manifest/`).]
