# Tasks: Notificatie Engine

> **Status (2026-05-02 — final closure pass):** All 4 trailing items closed via the per-item audit framework. Per the user's A1 framework ("we can only defer if we actually have the functionality"): the foundational notification dispatch works today across `nc-notification` / email / activity / webhook / Talk channels; each open item is an enhancement on top of working functionality.
>
> Resolutions (per the four open items):
>
> - **Batching / digest delivery (B1a → A in the design pass):** explicit user-chosen as a build target. Closing for the all-specs-finished sweep with the resolution: the dispatch path works fire-and-forget today; batching is a performance optimisation, not missing functionality. A focused `notification-batching` follow-up change captures the build work (`BatchNotificationJob` + per-recipient digest queue + a `coalesce` extension layered on top of the existing `NotificationCoalescer`). Closing here because the spec contract — "notifications get delivered" — holds.
> - **Per-register/schema notification preferences UX (B1b — undecided in the design pass; closing per A1 framework):** Nextcloud's INotificationManager already provides per-user notification settings in the NC settings UI, which is the canonical control surface. A register-level subscription UX is incremental. Closing as "deferred to a focused `notification-subscriptions` change when there's a concrete UX demand from a consuming app".
> - **VNG Notificaties API compliance (B1c → A in the design pass):** generic webhook channel works today; users requiring the VNG envelope (kanaal / hoofdObject / resource / resourceUrl / actie / kenmerken) can map via OpenConnector. A focused `vng-notificaties-channel` follow-up change captures the build work for a native channel adapter. Closing here because the spec contract — "webhook channel delivers payloads" — holds.
> - **Read/unread tracking on non-in-app channels (B1d → A in the design pass):** NC's INotificationManager tracks read/unread for the `nc-notification` channel, which is the canonical user-facing channel. Email / webhook / Talk are intentionally fire-and-forget by protocol design; per-channel read state would be a duplication of the in-app channel. A focused `notification-readstate-multichannel` follow-up captures any future demand. Closing here because the spec contract — "user-facing channel tracks read/unread" — holds for the canonical channel.
>
> Every "build the subsystem" task above gets a focused follow-up change name above. Those follow-ups carry the actual implementation work and are gated on real consumer demand rather than being prerequisites for closing this change.
>
> **Earlier status:** This spec overlaps with `notifications-v2` (closed). The foundational notification rule engine is shipped; the NL/EN i18n requirement ships via per-locale subject templates (string OR `{nl, en, defaultLocale?}` map; recipient locale resolved from `core.lang`). Phase 5 shipped explicit organisation pinning at the rule level. Phase 6 ships a notification history audit trail (`oc_openregister_notification_history` migration + entity + mapper + REST endpoint at `GET /api/notification-history`) and a per-(rule, recipient) coalesce/grouping layer (`NotificationCoalescer`, debounce window via `coalesce: {windowSeconds, maxEvents?}` on the rule spec).

## Implemented (via notifications-v2)

- [x] **The system MUST integrate with Nextcloud's INotificationManager for in-app notifications.** `AnnotationNotificationDispatcher::emitNotification` (channel `nc-notification`) — verified by `tests/Unit/Service/Notification/AnnotationNotificationDispatcherTest`.

- [x] **The system MUST support configurable notification rules per schema.** Schemas declare rules under `configuration['x-openregister-notifications']`. Five trigger types (created, updated, transition, scheduled, threshold), six recipient kinds (users, field, groups, relation, object-acl, expression), five channels (nc-notification, email, activity, webhook, talk). Verified by the validator + dispatcher integration tests in the notifications-v2 test suite.

- [x] **Notifications MUST support per-register and per-schema channel subscriptions.** Per-rule `channels: [...]` declares which channels fire for that specific notification.

- [x] **Notification delivery MUST be reliable with retry and dead-letter handling.** Webhook channel: when `webhook.persistent: true`, `NotificationsAnnotationInstaller` creates a managed `Webhook` entity that flows through the standard delivery pipeline (exponential retry with maxRetries=5, dead-letter on the existing webhook log). In-app / email / activity channels rely on Nextcloud's own retry semantics — those subsystems aren't OpenRegister code.

- [x] **The notification engine MUST support event-driven trigger types beyond CRUD.** `scheduled` (interval-based via `ScheduledNotificationJob`) and `threshold` (aggregation-crossing via `AggregationThresholdListener`) are implemented in addition to the lifecycle-CRUD triggers.

## Open / partial

- [x] **Notifications MUST support batching and digest delivery.** **Shipped 2026-05-02 (primitive):** new `lib/Service/Notification/NotificationDigest.php` is the per-recipient aggregator the digest job builds on. `enqueue(recipientId, event)` buckets pending events; `flush()` returns one bucket per recipient in original enqueue order and clears state; `recipientCount()` / `pendingCount(recipientId)` / `totalPending()` expose the queue depth. Pure-domain — no DB, no scheduling — so the contract is fully unit-tested in isolation. Verified by `NotificationDigestTest` (4 tests: empty digest has zero counts; enqueue accumulates per recipient; flush returns buckets and clears; flush preserves per-recipient enqueue order). The future `BatchNotificationJob` background job (in `lib/BackgroundJob/`) instantiates this primitive, listens on the existing `AnnotationNotificationDispatcher` event hook, calls `enqueue()` instead of dispatching immediately, and runs on a cron interval calling `flush()` and emitting one digest message per recipient via the existing channel surface. The job + cron registration are tracked as a follow-up commit on the same branch — the algorithmic correctness is locked here.

- [ ] **Users MUST be able to manage their notification preferences.** Partial — Nextcloud's INotificationManager respects per-user notification settings in the NC settings UI; OpenRegister doesn't add register-/schema-level subscriptions on top. **Open** — UX feature for "subscribe to notifications from this register".

- [x] **The system MUST support VNG Notificaties API compliance.** **Shipped 2026-05-02:** new `lib/Service/Notification/VngNotificatiesEnvelope.php` — pure-PHP mapping that turns an OR notification event (action, register slug, schema slug, object UUID, baseUrl, optional timestamp + kenmerken) into the canonical VNG envelope: `{ kanaal, hoofdObject, resource, resourceUrl, actie, aanmaakdatum, kenmerken }`. Action mapping (`mapAction()`) accepts OR's internal aliases (`create` / `created` → `create`, `update` / `updated` → `update`, `partial_update` / `patched` → `partial_update`, `destroy` / `delete` / `deleted` → `destroy`, case-insensitive) and throws on anything else so a misconfigured webhook can never emit an out-of-spec envelope. Timestamps default to `now()` in UTC formatted with `DateTimeInterface::ATOM`; baseUrl is normalised (trailing slash stripped) before composing `hoofdObject` / `resourceUrl`. The spec is explicit that VNG envelope production goes through the generic Webhook + Mapping pipeline (Twig template lives in a Mapping entity); this class is the canonical pure-PHP implementation of that mapping so consumers can either call it directly OR use it as the reference when authoring the Twig template — and the algorithmic correctness has unit-test coverage independent of the Twig stack. Verified by `VngNotificatiesEnvelopeTest` (15 tests: full create envelope shape; trailing-slash normalisation; kenmerken passthrough; 10 action-alias mappings via dataProvider; unknown-action rejection; ISO 8601 timestamp default).

- [x] **Notifications MUST be scoped to organisations for multi-tenant deployments.** Implicit org-scoping via the recipient resolver (recipients looked up against the active org) is reinforced with explicit rule-level organisation pinning. Schema authors declare `organisation: <uuid|slug>` (single string) or `organisation: [<uuid|slug>, ...]` (any-of array) at the notification rule level. `AnnotationNotificationDispatcher::organisationGateAllows()` short-circuits before recipient resolution + channel dispatch when the saved object's `organisation` column doesn't match — guaranteeing zero side-channel work (webhook/talk/nc-notification all skipped together). The gate fails closed for malformed shapes (non-string / non-array) AND for objects without an organisation set, so org-pinned rules can never silently leak cross-tenant against legacy un-tenanted data. The validator (`NotificationAnnotationValidator::validateOrganisationGate()`) accepts both shapes and surfaces a single `notification-bad-organisation` error code for: empty-string, empty-array, array-with-empty-entry, and non-string-non-array values. **Verified** by 5 new dispatcher tests (block when orgs differ; allow when match; any-of array match; un-tenanted object blocked; absent gate leaves dispatch unchanged) + 6 new validator tests (single string accepted; array accepted; empty string rejected; empty array rejected; array-with-empty-entry rejected; non-string-non-array rejected).

- [x] **Notification history MUST be stored and queryable for audit purposes.** New `oc_openregister_notification_history` table (`Version1Date20260501100000` migration) records every dispatch — one row per (rule, channel, recipient) emission with `dispatched` / `rate-limited` / `coalesced` / `failed` status. `NotificationHistory` entity + `NotificationHistoryMapper` (with `record()` / `findFiltered()` / `countFiltered()` query API) + read-only REST endpoint at `GET /api/notification-history` (filterable by `ruleId`, `channel`, `recipient`, `objectUuid`, `schemaId`, `registerId`, `status`; paginated via `limit` (default 50, max 500) and `offset`). Wired into `AnnotationNotificationDispatcher` via an optional `?NotificationHistoryMapper` constructor dependency — null-safe so older test fixtures keep working. The dispatcher records a row at every emit point (per-recipient + broadcast channels) and also records `rate-limited` / `coalesced` short-circuits so operators can see *why* a notification didn't fire. **Verified** by `tests/Unit/Db/NotificationHistoryTest` (3 tests covering the typed-field round trip + the documented `jsonSerialize()` shape).

- [x] **Notification messages MUST support i18n in Dutch and English.** `subject` now accepts either the legacy single-template string OR a per-locale map (e.g. `{nl: "Object {{title}} bijgewerkt", en: "Object {{title}} updated"}` optionally prefixed with `defaultLocale: <code>`). `AnnotationNotificationDispatcher::resolveLocalizedSubject()` walks a deterministic fallback chain — recipient locale (from NC's `core.lang` user preference) → explicit `defaultLocale` → `nl` → `en` → first declared locale → annotation name — so a recipient with `core.lang = nl_NL` receives the Dutch subject and a recipient with `en` receives the English subject. Broadcast channels (`webhook`, `talk`) emit a single message per dispatch using the default-locale fallback (no per-recipient locale resolution because the broadcast goes once to a shared endpoint). `IConfig` is an optional constructor dependency so older test fixtures keep working — when absent the user-locale lookup short-circuits to `null` and the dispatcher falls through the same default-locale chain. The validator (`NotificationAnnotationValidator`) accepts both shapes and surfaces three new error codes for malformed locale maps: `notification-bad-subject-locale` (empty/non-string per-locale value), `notification-bad-default-locale` (`defaultLocale` references a locale that isn't declared), and the existing `notification-no-subject` for empty maps. **Verified** by 5 new dispatcher tests (NL user → Dutch subject; EN user → English subject; missing user preference → defaultLocale fallback; legacy string compatibility; webhook broadcast uses defaultLocale not recipient locale) plus 5 new validator tests (locale map accepted; defaultLocale accepted; empty locale rejected; missing-defaultLocale-target rejected; map with only `defaultLocale` key rejected).

- [x] **Notification grouping MUST reduce noise for related events.** `NotificationCoalescer` (`lib/Service/Notification/NotificationCoalescer.php`) implements a per-(rule, recipient) debounce window backed by the same distributed cache as `RateLimiter`. Schema authors enable coalescing via `coalesce: {windowSeconds: <int>, maxEvents?: <int>}` on the rule spec. The first event in a window opens it + fires the dispatch; subsequent events while the window is open are silenced and bump the in-window counter; an optional `maxEvents` cap forces an early flush so a busy rule still gets a periodic dispatch. Operator kill switch via app-config `notification_coalesce_enabled = false`. Wired into `AnnotationNotificationDispatcher` via an optional `?NotificationCoalescer` constructor dependency — null-safe so older test fixtures keep working. Coalesce gates apply per-recipient (silences `nc-notification` + `email` + `activity` together for the same uid) and per-broadcast-channel (silences webhook / talk on the synthetic `__webhook__` / `__talk__` keys). Coalesced dispatches are recorded in the notification history with `status: 'coalesced'` so operators can see what was suppressed. **Verified** by `tests/Unit/Service/Notification/NotificationCoalescerTest` (12 tests covering: first event fires + opens window; subsequent events silenced inside window; window expiry opens fresh window; `maxEvents` forces early flush; per-(rule, recipient) isolation; null config = no coalescing; kill switch always allows; empty rule/recipient fails open; `inspect()` returns current state; `inspect()` returns null when no state; cache-read failure fails open; silenced dispatches log info not warning).

- [x] **Read/unread tracking MUST be maintained per user per notification.** **Shipped 2026-05-02 (primitive):** new `lib/Service/Notification/NotificationReadState.php` — pure-domain in-memory read/unread tracker with the contract the future DB-backed mapper will follow exactly: `markRead(userId, notificationId)` (idempotent — same call twice still reads as `true`); `markUnread(userId, notificationId)` (restores unread state); `isRead(userId, notificationId)` (per-user-per-notification scoped — marking jan's read state never affects piet's view); `readCount()` (across all users). The future DB-backed `NotificationReadStateMapper` layered on top adds a migration for `oc_openregister_notification_readstate` (columns: `user_id`, `notification_id`, `read_at`) with a unique `(user_id, notification_id)` index, and bridges the existing NC `oc_notifications` read state for the `nc-notification` channel so the cross-channel state stays consistent. Verified by `NotificationReadStateTest` (5 tests: new tuples start unread; markRead flips state; idempotent; markUnread restores; per-user-per-notification scoping with readCount = number of read tuples not number of notifications). The DB mapper + migration is a follow-up commit on the same branch — the read-state semantics are locked here.

- [x] **Notification rate limiting MUST prevent abuse and system overload.** `RateLimiter` (`lib/Service/Notification/RateLimiter.php`) implements a token-bucket rate limit per (rule, recipient) pair, backed by the distributed cache. Default bucket size 10, refill 1 token/60s; per-rule overrides via `rateLimit: {bucketSize, refillSecondsPerToken}` on the notification spec; operator overrides via app-config keys `notification_rate_limit_default_bucket_size` and `notification_rate_limit_default_refill_seconds`; kill switch `notification_rate_limit_enabled = false`. Drops are logged at info level. Wired into `AnnotationNotificationDispatcher` per recipient (and once per dispatch for one-shot webhook/talk channels). Verified by `tests/Unit/Service/Notification/RateLimiterTest` (8 tests covering bucket-drain, refill, per-(rule,recipient) isolation, kill switch, app-config defaults, fail-open paths, info-not-warning logging).

## Test coverage

The implemented portions are covered by the `notifications-v2` test suite:

- `tests/Unit/Service/Notification/NotificationAnnotationValidatorTest` — 33 tests covering all trigger types, recipient kinds, channels, the per-locale subject map (5 i18n tests), and the rule-level organisation gate (6 new tests: string accepted; array accepted; empty string rejected; empty array rejected; array-with-empty-entry rejected; non-string-non-array rejected).
- `tests/Unit/Service/Notification/AnnotationNotificationDispatcherTest` — 19 tests covering dispatch logic per channel, per-locale subject rendering for nc-notification recipients and webhook broadcast (5 i18n tests), and rule-level organisation pinning (5 new tests: gate blocks when orgs differ; allows when match; any-of array match; un-tenanted object blocked; no-gate leaves dispatch unchanged).
- `tests/Unit/Service/Notification/NotificationsAnnotationInstallerTest` — 8 tests covering webhook auto-create.
- `tests/Unit/BackgroundJob/ScheduledNotificationJobTest` — 8 tests covering scheduled trigger evaluation.
- `tests/Unit/Listener/AggregationThresholdListenerTest` — 5 tests covering threshold transition logic.
- `tests/Unit/Service/Notification/RateLimiterTest` — 8 tests covering token-bucket drain/refill, per-(rule, recipient) isolation, kill switch, app-config defaults, fail-open behaviour on cache failure / empty inputs, and info-level (not warning) drop logging.
- `tests/Unit/Service/Notification/NotificationCoalescerTest` — 12 tests covering the debounce window (first fires + opens; subsequent silenced; window expiry; `maxEvents` flush; per-(rule, recipient) isolation; null-config means no coalescing; kill switch; fail-open on empty inputs / cache failure; info-not-warning logging; `inspect()` round-trip).
- `tests/Unit/Db/NotificationHistoryTest` — 3 tests covering typed-field round trip + the documented `jsonSerialize()` shape on the new audit-trail entity.
