# Tasks — or-mcp-derived-tool-provider

- [ ] Create `lib/Mcp/BuiltIn/SchemaDerivedToolProvider.php` implementing `IMcpToolProvider` (SPDX docblock); constructor takes an owning `appId` + that app's opted-in schemas; `getAppId()` returns the owning appId (one instance per owning app)
- [ ] Implement `getTools()`: enumerate `enabled:true` schemas, emit `{appId}.{schema}.{verb}` per declared verb, reuse the schema as inputSchema/outputSchema, attach dialect `description`/default and pass through the untrusted MCP hints
- [ ] Implement `invokeTool()` dispatch by parsed `{appId}.{schema}.{verb}`
- [ ] search: honour declared `filters` only (reject undeclared keys), bounded pagination (default + hard max/clamp), optional field projection, truncation defaults, has-more/total metadata
- [ ] get: fetch single object by id/uuid via `ObjectService`
- [ ] create/update/delete: write through `ObjectService` in the ambient session — no impersonation, no system account, RBAC + IDOR enforced by `ObjectService`
- [ ] Register one derived provider per opted-in app in `Application::registerMcpToolProviders()`, ordered AFTER hand-written per-app providers (JSON-RPC first-wins precedence)
- [ ] Bridge the derived providers into `ToolRegistry` via the existing `ToolRegistrationListener`/`McpProviderBridge` so they appear on the facade surface
- [ ] Implement self-suppression on both surfaces: skip a derived id already exposed by a hand-written provider for the same appId
- [ ] Emit exactly one immutable audit record per invocation (acting identity, toolId, params digest — never raw args, result summary) via `AuditTrail`/`AuditHashService`; audit failed/unauthorized invocations too, and cross-reference the object-level `AuditTrail` row for writes
- [ ] Add test fixtures: one opted-in schema (all five verbs) + one hand-written provider claiming a colliding id (precedence test)
- [ ] Test getTools(): correct ids; disabled/absent schemas emit nothing; `tools` subset narrows verbs
- [ ] Test both surfaces: derived tool present in `McpToolsService::listTools()` and `ToolRegistryFacade::listTools()`; both route to the same invokeTool
- [ ] Test precedence: hand-written wins, derived duplicate suppressed; non-colliding derived verbs still emit
- [ ] Test search: declared-filter-only, bounded pagination/clamping, projection, truncation + has-more
- [ ] Test RBAC: authorized write succeeds via ObjectService; unauthorized write returns isError with no bypass
- [ ] Test audit: one record per invocation (read + write + failed), params digest not raw, chained into audit trail
- [ ] Update `ai-mcp` + `mcp-discovery` capability specs with the new requirements
- [ ] Run `composer check:strict` and fix any pre-existing issues touched
