# Tasks — or-mcp-tool-attribute

- [ ] Create the net-new attribute `lib/Mcp/Attribute/McpTool.php` (`Attribute::TARGET_METHOD`, SPDX docblock) with optional `name` + `description`
- [ ] Implement inputSchema inference from parameter type hints + docblock `@param` (scalars/array/nullable → JSON-schema types; no-default params → required)
- [ ] Implement best-effort outputSchema inference from return type / `@return`
- [ ] Create the reflection scanner (`lib/Mcp/AttributeToolScanner.php` or equivalent) that finds public `#[McpTool]` methods in each app's declared scannable service classes
- [ ] Ignore non-public attributed methods with a logged warning
- [ ] Build a descriptor `{appId}.{toolName}` per attributed method and wrap attributed tools in a thin per-app `IMcpToolProvider`-shaped adapter
- [ ] Wire the scanner into `Application`'s MCP discovery so attributed tools register on BOTH surfaces (`McpToolsService` + `ToolRegistry`/`McpProviderBridge`) via the derived-provider path
- [ ] Implement in-process invocation: resolve the owning app's service from its own DI container and call the method in-process in the ambient session — no HTTP/RPC, no OR re-implementation (ADR-041)
- [ ] Enforce "no impersonation, no elevation"; the owning method owns its authorization/IDOR
- [ ] Reuse the derived provider's audit path: one immutable record per invocation (acting identity, toolId, params digest — not raw args, result summary); audit read/write/failed invocations
- [ ] Reject attributed↔derived id collisions at discovery with a developer-facing logged error; keep hand-written-wins precedence for hand-written collisions
- [ ] Add fixtures: a service with a fully typed+docblocked `#[McpTool]` method, a minimal defaulted one, and a non-public attributed method
- [ ] Test attribute defaults: name → method name, description → docblock summary
- [ ] Test inputSchema inference (required vs optional/nullable params) and outputSchema best-effort
- [ ] Test scanner: attributed public method registered as `{appId}.{toolName}` on both surfaces; non-public ignored
- [ ] Test in-process invocation resolves and calls the owning app's method with no cross-app HTTP/RPC
- [ ] Test authorization: unauthorized invocation rejected by the owning method; no OR bypass/impersonation
- [ ] Test audit: one record per invocation (read/write/failed), params digest not raw, chained into the same audit trail as derived tools
- [ ] Update `ai-mcp` + `mcp-discovery` capability specs with the new requirements
- [ ] Run `composer check:strict` and fix any pre-existing issues touched
