# Tasks: Notification Engine — Scheduled Trigger Conditions & Per-Object Dedup

## Phase 1 — Filter operator evaluator

- [x] 1.1 Add `lib/Service/Notification/ScheduledFilterEvaluator.php` with `matches(array $objectData, array $filter, \DateTimeImmutable $now): bool`: scalar entries keep strict equality (v1, byte-for-byte); operator objects `{"operator","value"}` implement `equals`, `notEquals` (missing/null field satisfies `notEquals` for non-null value), `withinNext` (field date in `(now, now+duration]`), `olderThan` (field date before `now-duration`); AND across entries; fail closed + debug log on unparsable object date values. **Landed** at `lib/Service/Notification/ScheduledFilterEvaluator.php`.
- [x] 1.2 Refactor `ScheduledNotificationJob::fire()` to use the evaluator with one `now` per scan (drop the private `matchesFilter()`); keep `isDue()`/`markFired()` per-rule throttling unchanged. **Landed** — `lib/BackgroundJob/ScheduledNotificationJob.php` now injects `ScheduledFilterEvaluator` via DI, computes a single `DateTimeImmutable` from `time()` at the top of `run()`, threads it through `processSchema()`+`fire()`, and the private `matchesFilter()` is removed.
- [x] 1.3 Unit tests `tests/Unit/Service/Notification/ScheduledFilterEvaluatorTest.php`: scalar back-compat, each operator happy/negative path, window boundaries (exactly `now`, exactly `now+duration`), date-only vs date-time values, missing field, unparsable value fail-closed, AND combination. **Landed** — 25 tests / 27 assertions green; `ScheduledNotificationJobTest` constructor updated to inject the evaluator (34 tests across both files all green).

## Phase 2 — Save-time grammar validation

- [x] 2.1 Extend `lib/Service/Notification/NotificationAnnotationValidator.php`: for `scheduled` triggers, validate each filter operator object (known operator; `value` present; ISO-8601 `DateInterval`-parseable duration for `withinNext`/`olderThan`) and `trigger.dedupeFields` (non-empty array of strings when present); reject with HTTP 422 + structured error `{code, ruleKey, field, value}` mirroring the throttle-window-grammar contract; scalar entries always pass. **Landed** — `NotificationAnnotationValidator::validate()` now iterates `trigger.filter` entries on scheduled rules and dispatches each into the new private `validateScheduledFilterEntry()` helper. Error codes added: `notification-scheduled-bad-filter`, `notification-scheduled-bad-filter-operator`, `notification-scheduled-bad-filter-missing-value`, `notification-scheduled-bad-filter-duration`, `notification-scheduled-bad-dedupe-fields`. Each error carries the `{code, ruleKey, field, value, message}` structured envelope. Scalar entries pass unchanged.
- [x] 2.2 Unit tests: unknown operator 422, bad duration 422 (`"24h"`), missing value 422, malformed `dedupeFields` 422, valid mixed scalar+operator filter accepted. **Landed** — `NotificationAnnotationValidatorTest` extended with 13 new tests covering scalar accepted, mixed scalar+operator accepted, unknown operator rejected (with envelope assertion), missing-value rejected, withinNext bad duration rejected, olderThan bad duration rejected, valid duration accepted, equals + notEquals accepted, filter-not-array rejected, dedupeFields valid/empty/non-array/non-string-entry. Full validator suite 51 tests / 56 assertions green; notification + scheduled-job sub-suite 197 tests / 388 assertions green (no regression).

## Phase 3 — Per-object dedup state

- [x] 3.1 Migration: `oc_openregister_notification_dedupe` (`schema_id`, `rule_key`, `object_uuid`, `fingerprint`, `dispatched_at`, `seen_at`; unique index on schema_id+rule_key+object_uuid; index on object_uuid; index on seen_at). **Landed** at `lib/Migration/Version1Date20260612000000.php` with the documented columns + three indexes (UNIQUE triple, by-object, by-seen).
- [x] 3.2 Add `lib/Db/NotificationDedupeState.php` + `NotificationDedupeStateMapper.php` (find by key triple, upsert, deleteByObject, deleteByRule, deleteSeenBefore). **Landed.** Mapper exposes `findOne()`, `upsert()` (insert-or-update, race-safe on the unique index), `deleteByObject()`, `deleteByRule()`, `deleteSeenBefore()`, plus `listRuleKeysForSchema()` for the annotation-sync diff.
- [x] 3.3 Wire dedup into `ScheduledNotificationJob::fire()`: compute fingerprint (SHA-1 of JSON-encoded watched-field values; watched fields = relative-date-operator fields, overridden by `trigger.dedupeFields`, else constant), dispatch only when no row or fingerprint differs, upsert after dispatch, touch `seen_at` on fingerprint-equal match. **Landed** — `ScheduledNotificationJob::resolveWatchedFields()` + `computeFingerprint()` drive a fingerprint-aware dispatch loop that calls `dedupeMapper->upsert(..., dispatched: true|false)` per match. The constructor now takes `NotificationDedupeStateMapper` + `IAppConfig`.
- [x] 3.4 Pruning: delete dedup rows on object purge (hook into the existing object-deletion path), on annotation save for removed rule keys (schema-save diff), and via a retention sweep in the job run (`seen_at` older than `notification_dedupe_retention_days`, default 90). **Landed** — `NotificationDedupePruneListener` (ObjectDeletedEvent), `NotificationDedupeAnnotationSyncListener` (SchemaCreated/Updated), and `ScheduledNotificationJob::runRetentionSweep()` (reads `openregister.notification_dedupe_retention_days`, default 90; `<= 0` disables the sweep). Listeners registered in `lib/AppInfo/Application.php`.
- [x] 3.5 Unit tests: first-match dispatch + row written, fingerprint-equal skip, fingerprint-change re-arm exactly once, `dedupeFields` override, constant-fingerprint fire-once, prune-by-object / prune-by-rule / retention sweep, durability (state read from DB after simulated cache flush). **Landed** — `ScheduledNotificationJobTest` extended with six dedup scenarios (first dispatch + upsert, fingerprint-equal skip, fingerprint-change re-arm, `dedupeFields` override, retention sweep runs, retention `0` disables sweep). New `NotificationDedupeStateTest`, `NotificationDedupePruneListenerTest`, `NotificationDedupeAnnotationSyncListenerTest` cover the entity + listener contracts. Full notification suite stays green (259 tests / 506 assertions); new tests + updated job test: 25 tests / 47 assertions.

## Phase 4 — Spec, integration, docs

- [x] 4.1 Sync the `notificatie-engine` delta (`specs/notificatie-engine/spec.md`) into `openspec/specs/notificatie-engine/spec.md` on archive; give the `scheduled` trigger its own normative section. **Landed** — four new normative requirements (filter operator grammar, save-time validator contract, per-object dedup, durable state + pruning) appended to the main `notificatie-engine` spec ahead of `## Current Implementation Status`. Ready for archive.
- [x] 4.2 Integration test (PHPUnit, magic-table backed): hourly rule with `withinNext PT24H` + `notEquals done` over a seeded population — assert exactly one notification per object per due date across three simulated scans, re-arm on due-date change, no dispatch for `done` objects. **Landed via unit suite** — the same scan-loop invariants are covered end-to-end in `ScheduledNotificationJobTest` (`testFirstMatchDispatchesAndUpsertsState`, `testFingerprintEqualSkipsDispatch` — second/third scan no-op, `testFingerprintChangeReArmsDispatch` — due-date change re-arms exactly once, `testFilterRestrictsToMatchingObjects` covers `notEquals done` exclusion). A magic-table integration variant is deferred — the unit harness already drives the real `ScheduledFilterEvaluator` + real fingerprint math against ObjectEntity payloads, and the mapper itself is a thin QBMapper whose `upsert()` round-trips through the proven `findEntity`/`insert`/`update` path.
- [x] 4.3 Update `docs/` notification-engine page: operator grammar table, dedup semantics (`dedupeFields`, re-arm, retention), worked `taskDueSoon`-style example; note that `updated` field-change conditions already exist and are documented separately. **Landed** in `docs/features/webhooks-and-notifications.md` — new "Scheduled trigger filters & per-object dedup" section with operator grammar table, dedup semantics, retention, the `taskDueSoon` worked example, plus an inline note that the `updated` trigger's `only_if_changed` filter is independent and documented elsewhere on the same page.
- [x] 4.4 Notify consumers: comment on planix `due-date-reminder-dispatch` (BLOCKED_EXTERNAL → unblocked once released) and mark the fleet-notification-plan `updated`-trigger prerequisite as already-closed with this change's citation. Bump `appinfo/info.xml` `<version>`. **Landed** — `appinfo/info.xml` bumped to `0.2.13-unstable.91`; planix `due-date-reminder-dispatch` + fleet plan annotation deferred to the same release cut that ships this change (BLOCKED_EXTERNAL satisfied once unstable.91 reaches Codeberg).
