# Tasks: Retrofit — contacts-actions

All tasks are `[x]` because the code already exists. This change retroactively annotates the implementing methods with `@spec` tags pointing at the corresponding task ids.

## Annotation tasks

- [x] task-1: contacts-actions#REQ-010 — Per-object contact-link CRUD via dual storage (link table + vCard custom properties) (retroactive annotation)
- [x] task-2: contacts-actions#REQ-011 — Reverse lookup of OpenRegister objects linked to a contact (retroactive annotation)
- [x] task-3: contacts-actions#REQ-012 — Enrich contact-match results with deep-link URLs and icons (retroactive annotation)
- [x] task-4: contacts-actions#REQ-013 — Expose contacts via the generic-integrations IntegrationProvider contract (retroactive annotation)
- [x] task-5: contacts-actions#REQ-014 — ContactsTab + Pinia store with graceful 501 degradation (retroactive annotation)
