# Tasks — or-mcp-derived-tool-provider

- [x] Create `lib/Mcp/BuiltIn/SchemaDerivedToolProvider.php` implementing `IMcpToolProvider` (SPDX docblock); constructor takes an owning `appId` + that app's opted-in schemas; `getAppId()` returns the owning appId (one instance per owning app)
- [x] Implement `getTools()`: enumerate `enabled:true` schemas, emit `{appId}.{schema}.{verb}` per declared verb, reuse the schema as inputSchema/outputSchema, attach dialect `description`/default and pass through the untrusted MCP hints
- [x] Implement `invokeTool()` dispatch by parsed `{appId}.{schema}.{verb}`
- [x] search: honour declared `filters` only (reject undeclared keys), bounded pagination (default + hard max/clamp), optional field projection, truncation defaults, has-more/total metadata
- [x] get: fetch single object by id/uuid via `ObjectService`
- [x] create/update/delete: write through `ObjectService` in the ambient session — no impersonation, no system account, RBAC + IDOR enforced by `ObjectService`
- [x] Register one derived provider per opted-in app in `Application::registerMcpToolProviders()`, ordered AFTER hand-written per-app providers (JSON-RPC first-wins precedence)
- [x] Bridge the derived providers into `ToolRegistry` via the existing `ToolRegistrationListener`/`McpProviderBridge` so they appear on the facade surface
- [x] Implement self-suppression on both surfaces: skip a derived id already exposed by a hand-written provider for the same appId
- [x] Emit exactly one immutable audit record per invocation (acting identity, toolId, params digest — never raw args, result summary) via `AuditTrail`/`AuditHashService`; audit failed/unauthorized invocations too, and cross-reference the object-level `AuditTrail` row for writes
- [x] Add test fixtures: one opted-in schema (all five verbs) + one hand-written provider claiming a colliding id (precedence test)
- [x] Test getTools(): correct ids; disabled/absent schemas emit nothing; `tools` subset narrows verbs
- [x] Test both surfaces: derived tool present in `McpToolsService::listTools()` and `ToolRegistryFacade::listTools()`; both route to the same invokeTool
- [x] Test precedence: hand-written wins, derived duplicate suppressed; non-colliding derived verbs still emit
- [x] Test search: declared-filter-only, bounded pagination/clamping, projection, truncation + has-more
- [x] Test RBAC: authorized write succeeds via ObjectService; unauthorized write returns isError with no bypass
- [x] Test audit: one record per invocation (read + write + failed), params digest not raw, chained into audit trail
- [x] Update `ai-mcp` + `mcp-discovery` capability specs with the new requirements
- [x] Run `composer check:strict` and fix any pre-existing issues touched
