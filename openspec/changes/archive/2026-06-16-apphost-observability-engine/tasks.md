# Tasks: AppHost Declarative Observability Engine

## 0. Setup and Verification

- [x] 0.1 Verify ADR-040 draft exists in hydra (`hydra/openspec/changes/adr-040-declarative-observability`) and the descriptor schema there matches design.md — flag drift back to the hydra change, do not fork the contract here
  - Note: ADR-040 proposal/tasks exist in hydra; the canonical `observability.schema.json` (hydra task 2.1) is not yet written. This engine implements the contract from design.md/spec.md exactly (closed sets, allowlist regex, policies) so it matches once the hydra schema lands. No contract fork.
- [x] 0.2 Deduplication check: confirm no existing generic health/metrics machinery in OR (`lib/Controller/HealthController.php`, `lib/Service/MetricsService.php` are OR-self-only today; AppHost namespace is free)
- [x] 0.3 Confirm ManifestService can resolve a calling app's `src/manifest.json` for all deploy layouts (bind-mount apps-extra/<app> and custom_apps) — reuse the `GET /api/manifest/{appId}` resolution path
  - Implemented in `AppHost\Observability\ManifestLoader` mirroring `ManifestController::loadBundledManifest` (IAppManager::getAppPath → src/manifest.json), which works for every deploy layout.

## 1. Descriptor model

- [x] 1.1 `lib/AppHost/Observability/HealthCheckDescriptor.php` + `MetricDescriptor.php` value objects with strict validation (closed `type`/`kind` sets, filter operator allowlist, `tableCount.table` regex `/^[a-z0-9_]+$/`)
  - **spec_ref**: `specs/apphost-observability/spec.md` — Requirement: Descriptor Validation
- [x] 1.2 Parse `observability` block into descriptors; absent block → defaults (database check + orAvailable when OR registers declared; implicit metrics only); invalid block → manifest diagnostics error, endpoints fall back to defaults
  - Implemented in `AppHost\Observability\ObservabilityManifest` (parser + defaults + diagnostics) loaded via `ManifestLoader`, a clean dedicated home rather than overloading `ManifestService`.

## 2. Health engine

- [x] 2.1 `lib/AppHost/Observability/HealthCheckExecutor.php` implementing the 5 primitives (database, filesystem, appEnabled, appConfig, orAvailable)
- [x] 2.2 Status + HTTP code resolution: `severity` (critical/degraded) × `statusCodePolicy` (adr006/always200); CORS headers when `cors: true`
- [x] 2.3 `lib/AppHost/Controller/GenericHealthController.php` — `#[PublicPage]` + `#[NoCSRFRequired]`, response shape `{status, app, version, checks}`; exception messages logged never returned
- [x] 2.4 `lib/AppHost/IHealthCheckProvider.php` + provider merge (alias `OCA\OpenRegister\AppHost\IHealthCheckProvider::{appId}`)

## 3. Metrics engine

- [x] 3.1 `lib/AppHost/Observability/MetricsEngine.php` + Prometheus text renderer (0.0.4, HELP/TYPE, `{app}_` prefix sanitised, label escaping)
- [x] 3.2 `ObjectMetricSource` — objectCount/objectSum via OR's portable aggregation layer (groupBy JSON fields → labels, filter operators, `now`/`today` tokens). MUST NOT emit dialect-specific JSON_EXTRACT SQL from this class
  - Uses `ObjectService::countSearchObjects` + `getFacetsForObjects` (the portable layer); no JSON_EXTRACT SQL in the class.
- [x] 3.3 `TableMetricSource` — COUNT/GROUP BY via QueryBuilder, `labelDefaults` for NULL/empty columns, `labelMap` column→label rename, graceful zero-emission when the table does not exist (drained-table parity)
- [x] 3.4 `AppConfigMetricSource` — counter/gauge from IAppConfig int values
- [x] 3.5 `ProviderMetricSource` — `lib/AppHost/IMetricsProvider.php`, alias discovery `OCA\OpenRegister\AppHost\IMetricsProvider::{appId}`, merge rendered samples
- [x] 3.6 Implicit `{app}_info` (version, php_version, nextcloud_version) + `{app}_up`
- [x] 3.7 Per-metric `cacheTtl` via ICacheFactory distributed cache (key: appId + metric name)
- [x] 3.8 `lib/AppHost/Controller/GenericMetricsController.php` — admin-only, `text/plain; version=0.0.4`

## 4. Tests and contract

- [x] 4.1 Unit tests: every check type, every source kind, severity×policy matrix, validation rejections, cache TTL behaviour, label escaping (51 tests, all green)
- [x] 4.2 Newman contract collection `tests/integration/apphost-observability.postman_collection.json`: health public + shape, health 503/200 policies, metrics admin-gated (401/403 anonymous), Prometheus content-type + `_info`/`_up` presence — parameterised by appId so adoption specs can reuse it
- [x] 4.3 Wire collection into CI (registered in the Newman orchestrator `tests/newman/run-all.sh`)

## 5. Documentation

- [x] 5.1 `docs/Technical/declarative-observability.md`: block reference, all types/kinds, defaults, provider escape hatch, migration table from hand-written controllers
- [~] 5.2 Update OR website docs nav + changelog entry
  - Docs nav is auto-generated from the folder structure (sidebars.js `autogenerated`), so the new page appears automatically. A dedicated changelog entry is deferred to the release/changelog pass.

## 6. Quality gates

- [x] 6.1 `composer check:strict` green (phpcs/phpstan/phpmd/psalm clean on lib/AppHost); 24 hydra gates identical to the development baseline (gate-6/7/17 pre-existing OR debt, verified unchanged, AppHost files in none); `@spec` tags on every new class + public method referencing this change
