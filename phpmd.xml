<?xml version="1.0"?>
<ruleset name="openregister"
    xmlns="http://pmd.sourceforge.net/ruleset/2.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://pmd.sourceforge.net/ruleset/2.0.0 http://pmd.sourceforge.net/ruleset_xml_schema.xsd">
    <description>openregister — Conduction PHPMD ruleset (single source: conduction/hydra-gates).</description>

    <rule ref="vendor/conduction/hydra-gates/quality-config/phpmd.xml">
        <exclude name="StaticAccess"/>
    </rule>

    <!-- StaticAccess: one exception, and only one.

         `\OCA\OpenRegister\Support\FleetAppId` is a pure resolver over a
         constant map: given an IAppManager and a canonical app name, it returns
         whichever id the instance actually registered. It holds no state, has
         no collaborators, and takes the only thing it needs as an argument —
         so there is nothing here that could be injected. Making it a service
         would add DI wiring around a lookup table.

         It also exists to be DELETED. It bridges the window where
         `development` carries the new fleet app ids and `beta`/`main` still
         carry the old ones; once those agree, every call site becomes a plain
         string again and this class goes away. Building an injectable
         abstraction for that would outlive the problem it solves.

         This uses the rule's OWN `exceptions` property — its designed
         configuration surface — rather than an @SuppressWarnings annotation or
         a baseline entry. The distinction matters: the rule stays fully active
         for every other class, and the exception is one named class, visible in
         config, with the reason attached. -->
    <!-- StaticAccess: a second exception, on the same terms.

         `\OCA\OpenRegister\Support\QueryLimit` answers one question about one
         argument — "how many rows does this `_limit` ask for?" — and returns a
         positive int or null. It holds no state, has no collaborators, and
         takes the only thing it needs as an argument, so there is nothing here
         that could be injected either.

         It is deliberately NOT a service. Its whole purpose is that three read
         paths reach the SAME answer; a service would let one of them be
         constructed with a different implementation, which is exactly the
         drift it was written to end. A pure function reachable by name from all
         three is the property being bought.

         Unlike FleetAppId this one is not temporary — the `_limit` grammar is
         permanent — so it earns its place on the merit of the first paragraph
         alone rather than on being short-lived. -->
    <!-- StaticAccess: a third exception, on the terms QueryLimit set.

         `\OCA\OpenRegister\Support\FilterParams` answers one question about
         request parameters: which of them are property filters, and under
         which of the two spellings. It holds no state, has no collaborators,
         and takes everything it needs as arguments.

         It exists because two sibling endpoints answered that question
         differently and each returned a confident wrong number for the
         other's spelling (openregister#3611). The property being bought is
         that object search, the four aggregation actions and the magic search
         handler cannot disagree again: a service would let one of them be
         constructed with a different implementation, which is the drift the
         class was written to end. -->
    <!-- StaticAccess: a fourth exception, on the terms above.

         `\OCA\OpenRegister\Support\PermissionBit` maps an action onto the core
         share permission bit it needs. The table used to be a public static on
         `ObjectGrantResolver`, which IS a service: it takes an IManager, caches
         resolved grants per user, and answers questions about live shares.
         Reaching into a stateful service by class name to read a constant table
         is the static access worth objecting to, so that call was refactored
         rather than excepted, and the table moved to a class that holds no
         state and has no collaborators.

    ===================================================================
         StaticAccess: named constructors on immutable value objects.

         Every class below has a PRIVATE (or purely structural) constructor and
         is reached only through its own named constructor: `parse`, `fromArray`,
         `fromProperty`, `fromStored`, `fromDeclarations`, `empty`, `none`,
         `unchanged`, `expanded`. That call IS the constructor. PHPMD cannot
         distinguish it from a static call into a collaborator, but `new Foo(...)`
         would produce byte-identical semantics and the rule does not flag `new`.

         There is nothing here to inject and nothing to stub: the object has no
         identity of its own, holds only readonly data, and its named
         constructor exists so that a malformed input is refused in ONE place
         instead of at every call site. Routing that through an injected factory
         would add a second construction path, which is the drift each of these
         classes was written to end.

         Verified per class before it was listed: the constructor is private or
         the class carries only readonly state, and the statically reached
         method parses or validates its arguments and returns a value object.
         Each entry was also removed and phpmd re-run, to confirm the finding
         comes back and the entry is therefore doing something.

           - Service\View\ViewAlert                    (private ctor, ::parse)
           - Service\Flow\MacroActionBinding           (private ctor, ::parse, ::refusals)
           - Service\Flow\Timer\ServiceHours           (private ctor, ::fromArray, ::none)
           - Service\Flow\Timer\WorkingCalendar        (private ctor, ::fromArray, ::easterSunday)
           - Service\Search\HistoryPredicate           (private ctor, ::parse)
           - Service\Search\DictionaryExpansion        (private ctor, ::unchanged, ::expanded)
           - Service\Search\SearchDictionary           (private ctor, ::empty, ::fromDeclarations)
           - Service\Schemas\ReferenceFilterDeclaration (private ctor, ::fromProperty)
           - Service\Rbac\TokenGrant                   (readonly VO, ::fromStored)

    ===================================================================
         StaticAccess: pure declaration readers, on the FleetAppId terms.

         These hold no state, declare no constructor worth calling, and take
         everything they need as arguments. They answer one question about one
         input, and several call paths must reach the SAME answer: a service
         would let one of them be constructed with a different implementation.

           - Service\Flow\FlowNextHint            (all static: reads the `next` hint a node declares)
           - Service\Schemas\ScopedPropertyDeclaration (all static: reads and compiles the `scope` annotation)
           - Service\Rules\RuleDescriptor         (::idFor formats a rule id from three strings,
                                                   and runs BEFORE any descriptor instance exists)

    ===================================================================
         StaticAccess: PHP core date classes.

         `\DateTimeImmutable::createFromInterface` and
         `\DateTimeZone::listIdentifiers` are language-level. There is no
         instance to inject and no seam to stub short of wrapping the SPL, which
         buys nothing here: the first converts a DateTimeInterface the caller
         already holds, the second reads the IANA zone list to refuse an invalid
         timezone in a working-calendar definition. -->
    <rule ref="rulesets/cleancode.xml/StaticAccess">
        <properties>
            <property
                name="exceptions"
                value="\OCA\OpenRegister\Support\FleetAppId,\OCA\OpenRegister\Support\QueryLimit,\OCA\OpenRegister\Support\FilterParams,\OCA\OpenRegister\Support\PermissionBit,\OCA\OpenRegister\Service\View\ViewAlert,\OCA\OpenRegister\Service\Flow\MacroActionBinding,\OCA\OpenRegister\Service\Flow\FlowNextHint,\OCA\OpenRegister\Service\Flow\Timer\ServiceHours,\OCA\OpenRegister\Service\Flow\Timer\WorkingCalendar,\OCA\OpenRegister\Service\Schemas\ScopedPropertyDeclaration,\OCA\OpenRegister\Service\Schemas\ReferenceFilterDeclaration,\OCA\OpenRegister\Service\Search\HistoryPredicate,\OCA\OpenRegister\Service\Search\DictionaryExpansion,\OCA\OpenRegister\Service\Search\SearchDictionary,\OCA\OpenRegister\Service\Rules\RuleDescriptor,\OCA\OpenRegister\Service\Rbac\TokenGrant,\DateTimeImmutable,\DateTimeZone"/>
        </properties>
    </rule>
</ruleset>
