{
  "openapi": "3.0.0",
  "info": {
    "title": "Credential Broker",
    "description": "Owner- and organisation-scoped credential metadata for the OpenRegister credential broker. Each object carries only a handle to a stored secret (name, provider, owner, scope, organisation, allowedApps) — never the secret value itself, which lives in the Nextcloud credential vault (ICredentialsManager) keyed by the object UUID (under the owning user for personal credentials, under the reserved system identity for organisation credentials). Materialised into OpenRegister by the credential-broker-service Repair step (OR does not self-import its own register JSON at boot — ADR-037).",
    "version": "1.4.0"
  },
  "x-openregister": {
    "type": "core",
    "app": "openregister",
    "openregister": "^v0.2.10",
    "description": "Foundational credential-metadata register shipped by OpenRegister for the credential broker (credential-broker chain). The provider catalogue and the outbound-broker code live outside this descriptor (lib/Settings/credential-providers.json + the credential-broker-service phase)."
  },
  "paths": {},
  "components": {
    "registers": {
      "credential-broker": {
        "slug": "credential-broker",
        "title": "Credential Broker",
        "version": "1.4.0",
        "description": "Owner- and organisation-scoped credential metadata. One object per stored credential; the secret is NOT here — it lives in the Nextcloud vault keyed by the object UUID (owning user for personal, reserved system identity for organisation).",
        "published": "2026-01-01T00:00:00+00:00",
        "schemas": [
          "brokeredcredential"
        ],
        "folder": "Open Registers/Credential Broker"
      }
    },
    "schemas": {
      "brokeredcredential": {
        "slug": "brokeredcredential",
        "title": "Credential",
        "version": "1.4.0",
        "published": "2026-01-01T00:00:00+00:00",
        "summary": "Owner- and organisation-scoped metadata for a brokered credential — a handle, never the secret.",
        "description": "Metadata for a secret the credential broker holds on a user's or organisation's behalf. Carries a name, the catalogue provider it targets, the owning user, the scope (personal|organisation), the owning organisation UUID for organisation credentials, and the app ids permitted to use it. MUST NOT contain the secret value: the token is stored in the Nextcloud credential vault (ICredentialsManager), encrypted at rest, keyed by this object's UUID — under the owning user for personal credentials and under a reserved system identity for organisation credentials. No property here is secret-bearing, so the metadata is safe to list, export, audit, and query.",
        "required": [
          "name",
          "provider"
        ],
        "properties": {
          "name": {
            "type": "string",
            "description": "Human-readable name the owner gives the credential (e.g. \"GitHub publisher\").",
            "maxLength": 255,
            "facetable": true,
            "title": "Name"
          },
          "provider": {
            "type": "string",
            "description": "Catalogue provider identifier the credential targets (e.g. \"github\", \"gitlab\") — a key in the read-only lib/Settings/credential-providers.json catalogue.",
            "maxLength": 64,
            "facetable": true,
            "title": "Provider"
          },
          "owner": {
            "type": "string",
            "description": "The owning user (OR-resolved owner). For a personal credential this is the access-control key: only the owner may read, use, or manage it, and the paired secret is stored under this user in the Nextcloud vault. For an organisation credential this records the provisioning administrator for attribution ONLY — access control is membership of `organisation`, never this field.",
            "maxLength": 255,
            "facetable": true,
            "title": "Owner"
          },
          "scope": {
            "type": "string",
            "enum": [
              "personal",
              "organisation"
            ],
            "default": "personal",
            "description": "Access-control scope. `personal` (the default when absent): owner-scoped, the secret lives under the owning user in the Nextcloud vault and only the owner may use or manage it. `organisation`: shared by an organisation, the secret lives under a reserved system identity keyed by this object's UUID and any member of `organisation` may drive a broker call through an allowed app; only an organisation administrator (or a Nextcloud admin) may create, update, or delete it.",
            "facetable": true,
            "title": "Scope"
          },
          "organisation": {
            "type": "string",
            "description": "The owning OpenRegister organisation UUID. REQUIRED when `scope` is `organisation` (membership of this organisation is the access-control key); MUST be absent/ignored for a personal credential, where it is never an access key.",
            "maxLength": 255,
            "facetable": true,
            "title": "Organisation"
          },
          "allowedApps": {
            "type": "array",
            "description": "App ids the owner has permitted to use this credential via the broker (the per-app allow-list the broker enforces).",
            "items": {
              "type": "string",
              "maxLength": 64
            },
            "title": "Allowed Apps"
          },
          "sharedWith": {
            "type": "array",
            "description": "Principals this credential is shared with, in addition to its scope. A share grants USE of the credential through the broker — never sight of the secret, which stays in the vault (ADR-004 Rule 1). Only the owner may change this list; a recipient can neither widen it nor re-share onward. A `group` entry is an RBAC permission principal ONLY and is never a tenant discriminator (ADR-002 Rule 1): a principal outside the credential's organisation is denied even when named here. An absent, empty, or malformed list grants nothing.",
            "items": {
              "type": "object",
              "required": [
                "type",
                "id"
              ],
              "properties": {
                "type": {
                  "type": "string",
                  "enum": [
                    "user",
                    "group"
                  ],
                  "description": "Whether `id` names a Nextcloud user or a Nextcloud group.",
                  "title": "Principal type"
                },
                "id": {
                  "type": "string",
                  "maxLength": 255,
                  "description": "The Nextcloud user id or group id.",
                  "title": "User or group"
                },
                "permission": {
                  "type": "string",
                  "enum": [
                    "use"
                  ],
                  "default": "use",
                  "description": "What the share grants. `use` lets the recipient drive a brokered call with this credential.",
                  "title": "Permission granted"
                }
              }
            },
            "title": "Shared With"
          },
          "sharedUsers": {
            "type": "array",
            "description": "DERIVED from `sharedWith` — do not set directly; the server recomputes it on every write and ignores any client-supplied value. It exists because the RBAC match operator `$contains` compares SCALARS, so it cannot test membership of the object-shaped `sharedWith` entries, and a match clause resolves whole tokens and cannot build `\"user:\" + $userId` from a prefixed list. Holding bare uids lets a rule say `{\"sharedUsers\": {\"$contains\": \"$userId\"}}`. A stale value here is an access-control bug in either direction, which is why it is never client-writable.",
            "items": {
              "type": "string",
              "maxLength": 255
            },
            "title": "Shared Users"
          },
          "sharedGroups": {
            "type": "array",
            "description": "DERIVED from `sharedWith` — see `sharedUsers`. Holds bare group ids so a rule can say `{\"sharedGroups\": {\"$contains\": \"$user.groups\"}}`, which matches when the object lists any one of the caller's groups.",
            "items": {
              "type": "string",
              "maxLength": 255
            },
            "title": "Shared Groups"
          },
          "kind": {
            "type": "string",
            "enum": [
              "secret",
              "oauth2-token-set"
            ],
            "default": "secret",
            "description": "The shape of the stored secret, MIRRORED from the provider catalogue for display and filtering. `secret` (the default when absent): one opaque string substituted into the provider's single auth header. `oauth2-token-set`: a whole OAuth2 token set the broker refreshes and rotates. The catalogue entry is authoritative: when this value and the entry disagree, the broker follows the entry and treats this as a stale mirror. It is never an input to which injection path runs, because this property is owner-writable and the catalogue is not.",
            "facetable": true,
            "title": "Kind"
          },
          "status": {
            "type": "string",
            "enum": [
              "pending",
              "active",
              "expired",
              "relink_needed",
              "disabled"
            ],
            "default": "active",
            "description": "Connection lifecycle for an `oauth2-token-set` credential. `pending`: a connection was started but no token set has been minted yet. `active`: usable. `expired`: past its expiry with no usable refresh token. `relink_needed`: the provider rejected the refresh with `invalid_grant`, so the grant is gone and a person must re-authorise; the object and its id are kept so every reference to it survives the repair. `disabled`: switched off locally, after a disconnect. A `relink_needed` or `disabled` credential fails every brokered call closed before any outbound request is made.",
            "facetable": true,
            "title": "Status"
          },
          "scopes": {
            "type": "array",
            "description": "The scopes the provider actually granted, as returned by the token response. Non-secret: it says what this connection may do, never how to do it.",
            "items": {
              "type": "string",
              "maxLength": 255
            },
            "title": "Granted Scopes"
          },
          "account": {
            "type": "object",
            "description": "The provider account this connection belongs to. Non-secret identity only, used to label the connection in the UI and to stop a re-authorisation landing on a different account by mistake.",
            "properties": {
              "id": {
                "type": "string",
                "maxLength": 255,
                "description": "The provider's own identifier for the account.",
                "title": "Account id"
              },
              "handle": {
                "type": "string",
                "maxLength": 255,
                "description": "The account handle as the provider spells it (for example `@example@mastodon.example`).",
                "title": "Handle"
              },
              "displayName": {
                "type": "string",
                "maxLength": 255,
                "description": "The account's display name at the time of connecting.",
                "title": "Display name"
              }
            },
            "title": "Account"
          },
          "expiresAt": {
            "type": "string",
            "format": "date-time",
            "description": "When the stored access token expires. Mirrored from the token set so a listing can show a connection's health without touching the custody leaf.",
            "title": "Expires At"
          },
          "lastRefreshedAt": {
            "type": "string",
            "format": "date-time",
            "description": "When the token set was last rotated. Written only after the custody write succeeded, so it never claims a token the leaf does not hold.",
            "title": "Last Refreshed At"
          },
          "lastError": {
            "type": "string",
            "maxLength": 1024,
            "description": "The last refresh or revoke failure, secret-free by construction: an error code and a redacted reason, never a token, a client secret, or a provider response body.",
            "title": "Last Error"
          },
          "instanceBaseUrl": {
            "type": "string",
            "format": "uri",
            "maxLength": 512,
            "description": "The API host for a provider whose catalogue entry declares `baseUrlFrom: \"instanceBaseUrl\"` (Mastodon, Bluesky), where the host belongs to the connected account rather than to the provider. Validated once at mint (absolute https, no userinfo, no query, no fragment, a registrable domain rather than an IP literal, and never a loopback or private-range address) and IMMUTABLE afterwards: an update that changes it is rejected. This is the host-lock for such a credential, so a change here would be a change of security posture, not of configuration.",
            "facetable": true,
            "title": "Instance Base URL"
          },
          "clientId": {
            "type": "string",
            "maxLength": 255,
            "description": "The OAuth2 client identifier this connection was authorised with. Non-secret by definition, and recorded so a re-authorisation uses the same client. The matching client SECRET is never here; it is a separate brokered credential named by `clientCredentialRef`.",
            "title": "Client Id"
          },
          "clientCredentialRef": {
            "type": "string",
            "maxLength": 255,
            "description": "UUID of the brokered `generic-oauth2` credential holding the client secret used for this connection's token exchanges. Present when a tenant brought its own application, or when one was registered at the account's own server. Absent when the instance's administrator-set default client is used.",
            "title": "Client Credential Ref"
          },
          "createdAt": {
            "type": "string",
            "format": "date-time",
            "description": "When the credential was created.",
            "title": "Created At"
          }
        }
      }
    },
    "objects": [
      {
        "@self": {
          "register": "credential-broker",
          "schema": "brokeredcredential",
          "slug": "gemeente-example-github-publish"
        },
        "name": "Gemeente Example — GitHub publisher",
        "provider": "github",
        "owner": "00000000-0000-0000-0000-000000000000",
        "allowedApps": [
          "hermiq"
        ],
        "createdAt": "2026-01-01T00:00:00+00:00"
      },
      {
        "$comment": "A worked example of the oauth2-token-set kind. It is deliberately `pending`: a seeded object cannot carry a token set, because the token set lives in the custody leaf and this file has no place to put one and should not. A real connection reaches `active` only through the connect flow.",
        "@self": {
          "register": "credential-broker",
          "schema": "brokeredcredential",
          "slug": "reisbureau-example-mastodon-company-account"
        },
        "name": "Reisbureau Example \u2014 Mastodon company account",
        "provider": "mastodon",
        "kind": "oauth2-token-set",
        "status": "pending",
        "scope": "organisation",
        "organisation": "00000000-0000-0000-0000-000000000000",
        "owner": "00000000-0000-0000-0000-000000000000",
        "instanceBaseUrl": "https://mastodon.example",
        "scopes": [
          "read:accounts",
          "write:statuses"
        ],
        "account": {
          "id": "",
          "handle": "",
          "displayName": ""
        },
        "allowedApps": [
          "pipelinq"
        ],
        "createdAt": "2026-01-01T00:00:00+00:00"
      },
      {
        "@self": {
          "register": "credential-broker",
          "schema": "brokeredcredential",
          "slug": "reisbureau-example-gitlab-discovery"
        },
        "name": "Reisbureau Example — GitLab discovery",
        "provider": "gitlab",
        "owner": "00000000-0000-0000-0000-000000000000",
        "allowedApps": [
          "openbuild"
        ],
        "createdAt": "2026-01-01T00:00:00+00:00"
      }
    ]
  }
}
